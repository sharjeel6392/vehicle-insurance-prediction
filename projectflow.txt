1. Create a project template by executing template.py file. This will create the basic structure of files and directories.
2. Write the code for setup.py and pyproject.toml files to import local packages
3. Create a virtual env, activate it and install the requirements from requirements.txt
    conda create -n proj1 python=3.10 -y
    conda activate proj1
    add required modules to requirements.txt
    Do "pip install -r requirements.txt"
4. Do a "pip list" on terminal to make sure you have all the local packages installed.

------------------------------------------------- MongoDB Setup ---------------------------------------------------------------

5. Sign up to MongoDB Atlas and create a new project by just providing it a name then next => next => create.
6. From "Create a cluster" screen, select "create", select M0 service keeping other services as default. Hit "create deployment"
7. Setup the username and password and create DB user.
8. Go to "Network Access" and add ip address - "0.0.0.0/0" so we can access it from anywhere.
9. Go back to project >> "Get connection string" >> "Drivers" >> {Driver:Python, Version:3.6 or later}
    >> copy and save the connection string with you(replace password). >> Done.
10. Create a folder "notebook" >> create file "mongoDB_demo.ipynb >> select kernel >> python kernel >> visa
11. Kaggle data link: https://kaggle.com/datasets/moro23/easyvisa-datasets
12. Push the data to mongoDB database from python notebook
13. Go to mongoDB atlas >> database >> browse collection >> see the data in key-value format.


sharjeel6392
1nts8f5YtRwCgv9Y
connection string: mongodb+srv://sharjeel6392:<db_password>@cluster0.qvdpoh5.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0